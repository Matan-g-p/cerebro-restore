# =============================================================================
# Cerebro Restore - Environment Configuration Template
# =============================================================================
# Copy this file to .env and update with your actual values
# The .env file is gitignored and will not be committed to version control
#
# IMPORTANT: After copying, update all values below with your credentials
# =============================================================================

# -----------------------------------------------------------------------------
# Airflow Metadata Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Database used by Airflow for storing DAG metadata, task history, etc.
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# -----------------------------------------------------------------------------
# Data Lake Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Separate database for storing raw and transformed data
DATALAKE_USER=admin
DATALAKE_PASSWORD=admin
DATALAKE_DB=cerebro

# -----------------------------------------------------------------------------
# MinIO Object Storage
# -----------------------------------------------------------------------------
# S3-compatible object storage for raw data files
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# -----------------------------------------------------------------------------
# Airflow Web UI Admin Credentials
# -----------------------------------------------------------------------------
# Default admin user for accessing Airflow web interface
AIRFLOW_WWW_USER_USERNAME=airflow
AIRFLOW_WWW_USER_PASSWORD=airflow

# -----------------------------------------------------------------------------
# Airflow User ID
# -----------------------------------------------------------------------------
# User ID for file permissions (default: 50000)
# On Linux/Mac, you can set this to your user ID with: echo $UID
AIRFLOW_UID=50000
